[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "comfyui-weetodd-nodes"
version = "0.1.0"
description = "WeeTodd Studio: standalone AI video and image creation with Draw Things, native MLX, and maintained ComfyUI nodes"
readme = "README.md"
requires-python = ">=3.11"
license = {file = "LICENSE"}
authors = [{name = "wee-todd"}]
dependencies = [
  "jsonschema>=4.23,<5",
  "mlx>=0.32.2",
  "mlx-vlm>=0.4.4",
  "numpy>=1.26",
  "opencv-python-headless>=4.10",
  "pillow>=11",
  "safetensors>=0.4",
  "transformers>=4.57",
]

[project.optional-dependencies]
convert = ["onnx==1.22.0"]
docs = ["PyYAML>=6.0"]
preview-ane = ["coremltools==9.0"]
ltx = [
  "ltx-core-mlx @ git+https://github.com/dgrauet/ltx-2-mlx.git@91e6f6c9bd621ff2ae31adfee643e113d67d6ae8#subdirectory=packages/ltx-core-mlx",
  "ltx-pipelines-mlx @ git+https://github.com/dgrauet/ltx-2-mlx.git@91e6f6c9bd621ff2ae31adfee643e113d67d6ae8#subdirectory=packages/ltx-pipelines-mlx",
]

[project.urls]
Repository = "https://github.com/wee-todd/WeeTodd-Studio"
Issues = "https://github.com/wee-todd/WeeTodd-Studio/issues"

[tool.hatch.build.targets.wheel]
packages = [
  "src/wee_todd_nodes",
  "src/minimax_h3_mlx",
  "src/ltx23_mlx",
  "src/ltx25_mlx",
  "src/mlx_preprocessors",
  "src/wee_todd_mlx",
  "src/wee_todd_remote",
]

[tool.hatch.metadata]
allow-direct-references = true

[tool.pytest.ini_options]
pythonpath = ["src"]
testpaths = ["tests"]

[tool.ruff]
target-version = "py311"
line-length = 100
exclude = [
  "tests/test_chain_stitch.py",
  "tests/test_packing_parity.py",
  "tests/test_quant_roundtrip.py",
]

[tool.ruff.lint]
select = ["E", "F", "I", "UP", "B"]
